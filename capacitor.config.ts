import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'crud-test-fn',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
